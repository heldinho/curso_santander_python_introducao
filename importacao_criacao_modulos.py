import math

result = math.sqrt(25); print(result)

from math import sqrt

result_2 = sqrt(25); print(result_2)

from random import randint
from datetime import datetime

num_random = randint(1, 10); print(num_random)
date_now = datetime.now(); print(date_now)
